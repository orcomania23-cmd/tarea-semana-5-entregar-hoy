describir lo que ocurre en las rutas

confirma que el servidor funciona correctamente.

confirma que el servidor  funciona y emite el saludo que escribimos con anterioridad.

confirma que el servidor funciona correctamente y saluda en el idioma requerido.
